# Project structure

Current layout:

```text
INVOICE-LEDGER/
├── AGENTS.md
├── README.md
├── .gitignore
├── docs/
│   ├── conventions.md
│   └── structure.md
├── backend/
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── .env                 # Local secrets, ignored
│   ├── .env.example
│   ├── .venv/               # Regenerated by uv, ignored
│   ├── compose.yaml
│   ├── tests/
│   └── app/
│       ├── __init__.py
│       ├── cli.py
│       ├── db.py
│       ├── models.py
│       ├── schemas.py
│       ├── seed.py
│       └── services/
│           ├── __init__.py
│           └── suppliers.py
└── frontend/
    └── AGENTS.MD
```

Keep models in one file at this size. The unused models/ directory has been removed,
so only one definition of each table exists. Split into a package later only when
size warrants it; do not keep models.py and models/ together.

Planned additions, created only as each development stage starts:

```text
backend/app/
├── schemas.py               # Pydantic input/output validation, shared where useful
├── services/                # Accounting, payments, FX lookup, and reporting logic
├── seed.py                  # Explicit reference/demo seed
├── main.py                  # FastAPI application assembly
├── api/                     # Thin HTTP routes calling services
└── static/                  # Generated React production build, ignored
frontend/
├── package.json
├── package-lock.json
├── index.html
├── tsconfig.json
├── vite.config.ts
└── src/
    ├── main.tsx
    ├── App.tsx
    ├── components/
    ├── views/
    └── api/
```

Models describe stored SQLAlchemy tables; schemas describe validated input and
output. schemas.py now validates supplier creation and serializes supplier responses.
Extend it for invoice/payment inputs and reuse validation in CLI and API. A schemas/ package is unnecessary now.

Controllers normally coordinate HTTP requests. Our FastAPI api/ routes serve that
role, calling shared services, so a separate controllers/ folder would duplicate
responsibilities. Both CLI and API use services; neither contains accounting logic.
API and frontend remain deferred until CLI workflows and reports are complete.

A root Dockerfile and deployment configuration can be added at deployment time to
build the frontend and copy it into backend/app/static. FastAPI serves the API and
static files from one web server; development may use Vite build-watch only.

uv is initialized only in backend. Build configuration sets module-name = "app"
and module-root = ""; command entry points target app.cli:main. No Python version
file is needed because pyproject.toml declares the supported version. The project
is named invoice-ledger even though its importable package is app.
